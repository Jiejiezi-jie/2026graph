# 路由标签规则与选择头实验总结

## 1. 实验目标

实验目标是为每道题选择一个 RAG 方法标签，训练一个选择头，根据问题自动路由到 `vector`、`lightrag` 或 `pathrag`。

官方评测结果来自 GraphRAG-Bench 的有效评测行。补回 `Medical-6b20ca96` 后，数据共 120 道题，按固定划分分为：

- 训练集：72 题
- 验证集：24 题
- 测试集：24 题

选择头使用问题文本或问题向量作为输入，标签来自三个方法在每道题上的 Answer Correctness、成本和规则筛选结果。

## 2. 标签规则

候选方法首先必须满足：

1. 方法 Answer Correctness 不低于设定阈值；
2. 方法 Answer Correctness 不低于本题最高正确率减去容差；
3. 在合格方法中选择输入 token 数与输出 token 数之和最低的方法作为银标。

如果没有方法合格，则使用本题正确率最高的方法作为回退标签，并标记为 `all_failed`。

本轮比较过以下规则：

| 规则 | 含义 |
|---|---|
| ≥0.50 | 正确率达到 0.50，合格方法中选择成本最低者 |
| ≥0.60，最高−0.05 | 正确率达到 0.60，且距离本题最高正确率不超过 0.05 |
| ≥0.60，最高−0.10 | 正确率达到 0.60，且距离本题最高正确率不超过 0.10 |
| ≥0.60，最高−0.15 | 正确率达到 0.60，且距离本题最高正确率不超过 0.15 |
| ≥0.60，最高−0.20 | 正确率达到 0.60，且距离本题最高正确率不超过 0.20 |

## 3. 选择头比较

5 折交叉验证按题型分层，开发集为训练集加验证集，共 96 题；超参数先按 5 折 Macro-F1、再按 5 折准确率选择。候选方案对比是在缺失题补回前完成的，因此下表保留当时的 23 题测试结果，便于审计原始选择过程。

检索后端彼此独立：Vector 不使用图；LightRAG 使用 LightRAG 构建的 3396 节点、5693 边图；PathRAG 使用 PathRAG 构建的 4568 节点、9432 边图。特征方法分为 TF-IDF 和 BGE-M3。

| 标签规则 | 特征 | 5折准确率 | 5折 Macro-F1 | 测试准确率 | 测试 Macro-F1 | 测试标签划分 V/L/P | 多数类准确率 |
|---|---|---:|---:|---:|---:|---:|---:|
| ≥0.50 | TF-IDF | 0.5521 | 0.3563 | 0.5652 | 0.3926 | 16/1/6 | 0.6957 |
| ≥0.50 | BGE-M3 | 0.5417 | 0.3629 | 0.6087 | 0.4286 | 16/1/6 | 0.6957 |
| ≥0.60，最高−0.05 | TF-IDF | 0.4375 | 0.4010 | 0.5217 | 0.4702 | 9/5/9 | 0.3913 |
| ≥0.60，最高−0.05 | BGE-M3 | 0.3854 | 0.3529 | 0.4348 | 0.3977 | 9/5/9 | 0.3913 |
| ≥0.60，最高−0.10 | TF-IDF | 0.3958 | 0.3638 | 0.5652 | 0.4960 | 11/4/8 | 0.4783 |
| ≥0.60，最高−0.10 | BGE-M3 | 0.4167 | 0.3759 | 0.3913 | 0.3610 | 11/4/8 | 0.4783 |
| ≥0.60，最高−0.15 | TF-IDF | 0.4271 | 0.3747 | 0.5217 | 0.3761 | 12/3/8 | 0.5217 |
| ≥0.60，最高−0.15 | BGE-M3 | 0.3750 | 0.3044 | 0.4783 | 0.4347 | 12/3/8 | 0.5217 |
| ≥0.60，最高−0.20 | TF-IDF | 0.4375 | 0.3594 | 0.4783 | 0.3644 | 13/2/8 | 0.5652 |
| ≥0.60，最高−0.20 | BGE-M3 | 0.3854 | 0.3121 | 0.5217 | 0.4709 | 13/2/8 | 0.5652 |

其中 `V/L/P` 分别表示测试集银标为 `vector/lightrag/pathrag` 的数量。

## 4. 最终采用的配置

最终采用：

```text
标签规则：Answer Correctness ≥ 0.60，且不低于本题最高正确率 − 0.10
合格方法：选择成本最低者
图来源：LightRAG/PathRAG 各自原生图（Vector 不使用图）
特征方法：TF-IDF
```

对应的选择头结果为：

```text
5折准确率：    0.3958
5折 Macro-F1： 0.3638
测试准确率：   0.5417
测试 Macro-F1：0.4744
测试标签划分： vector 11 / lightrag 4 / pathrag 9
测试预测划分： vector 14 / lightrag 4 / pathrag 6
多数类准确率： 0.4583
```

完整 24 题测试集的混淆矩阵（行是真实银标、列是预测，顺序均为 Vector/LightRAG/PathRAG）为：

```text
[[8, 3, 0],
 [1, 1, 2],
 [5, 0, 4]]
```

选择这一配置的主要原因：

1. 缺失题补回前，`≥0.60，最高−0.10 + TF-IDF` 的 23 题测试 Macro-F1 为 0.4960，是当时候选组合中最高的，因此冻结为最终方案；
2. 相比 BGE-M3，在同一标签规则下，TF-IDF 的测试准确率为 0.5652、Macro-F1 为 0.4960，均高于 BGE-M3 的 0.3913 和 0.3610；
3. 相比容差 0.05，容差 0.10 在原 23 题对比中使测试银标从 9/5/9 变为 11/4/8；补回的第 24 题按回退规则标为 PathRAG，因此最终分布为 11/4/9；
4. 容差扩大到 0.15 或 0.20 后，TF-IDF 测试 Macro-F1 分别下降到 0.3761 和 0.3644；
5. ≥0.50 规则虽然 BGE-M3 的测试准确率达到 0.6087，但标签分布高度偏向 `vector`，多数类准确率达到 0.6957，因此单看准确率会受到类别不平衡影响，Macro-F1 更低，不能作为更可靠的综合选择。

需要说明的是，最终选择参考了固定测试集上的对比结果，属于本轮实验中的经验性选择；如果要进行严格无偏的最终报告，应只使用验证集选择规则和特征方法，再将测试集作为一次性报告指标。

## 5. 结果文件

完整 120 题最终规则对应的原始结果和银标文件位于：

```text
results_api/p1/analysis_complete_120_margin_010/
```

其中包括：

- `silver_labels.jsonl`：银标标签
- `summary.json`：标签规则、路由器和各策略指标
- `tfidf_5fold/summary.json`：最终 TF-IDF 五折及 24 题测试结果
- `tfidf_5fold/test_predictions.jsonl`：24 题测试预测
- `router_tuning.json`：选择头超参数搜索结果
- `router.joblib`：训练后的选择头
- `router_test_predictions.jsonl`：测试集预测结果
